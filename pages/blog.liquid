---js
{
  title: 'Blog',
  pagination: {
    data: 'collections.post',
    size: 4,
    alias: 'posts',
    reverse: true,
    before: function(data) {
      return data.filter((item) => item.data.permalink !== false);
    }
  }
}
---
<div class="container">
    {% render 'h1', text: title %}
    {% render 'bloglist', posts: posts%}
    {% render 'pagination', pagination: pagination %}
</div> 